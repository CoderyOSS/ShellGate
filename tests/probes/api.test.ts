import { describe, it } from "bun:test";

describe("api", () => {
  it.todo("GET /health returns 200 with status ok and db_connected");
  it.todo("GET /api/grants returns JSON array of seeded grants");
});
